{% extends 'base.html.twig' %}

{% block body %}

    <h3>P3 - a)</h3>
    {% for i in 1..20 %}
        <div> {{ title|upper }} -> loop {{ i }}</div>
    {% endfor %}

    <div class="col-lg-12">
            <div class="d-grid gap-2">
                <center>
                    <a href="{{ path('ejercicios') }}" class="btn btn-warning btn-lg mt-4"> Volver a Ejercicios</a>
                </center>
            </div>
        </div>
    
{% endblock %}