{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="col-lg-12">
            <div class="d-grid gap-2">
                <center>
                    <a href="{{ path('ejercicio2a') }}" class="btn btn-primary btn-lg mt-4"> Ejercicio 2a</a>
                    <a href="{{ path('ejercicio2b') }}" class="btn btn-info btn-lg mt-4"> Ejercicio 2b</a>
                    <a href="{{ path('login') }}" class="btn btn-success btn-lg mt-4"> Login</a>
                </center>
            </div>
        </div>
    </div>
    
{% endblock %}