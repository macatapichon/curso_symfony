{% extends 'base.html.twig' %}

{% block body %}

    <h3>P3 - b)</h3>

    {% if title == 'rojo' %}
        <div style="color: red;">Soy un div rojo</div>
    {% else %}
        <div style="color: blue;">Si no soy rojo, soy un div azul</div>
    {% endif %}

    <div class="col-lg-12">
        <div class="d-grid gap-2">
            <center>
                <a href="{{ path('ejercicios') }}" class="btn btn-warning btn-lg mt-4"> Volver a Ejercicios</a>
            </center>
        </div>
    </div>
    
{% endblock %}